# Memory Index — Turnocero

- [Claudio Hollman — developer profile](user_profile.md) — sole dev/owner, full-stack, Argentine Spanish UI, English commit messages
- [Turnocero current feature set](project_features.md) — Noticias, Compartidas, Torneos, Friends, public browsing, Sidebar/Navbar/GuestNavbar (BottomNav removed May 2026), admin moderation — features beyond CLAUDE.md
- [Calendario unificado](project_calendario_unificado.md) — sección `/calendario` (2026-06-02): agrega mesas+eventos+torneos en grilla+agenda, read-only, `Torneo.fecha` opcional, calendarService
- [Math Trade](project_math_trade.md) — sección `/math-trade` (mergeada a master 2026-06-02): intercambios múltiples por ciclos, want lists por título BGG, motor `mathTradeMatching.js` con 3 modos (max=Hungarian exacto / bounded=NP-hard peeling / auto=cadena más corta sin perder), `MathTrade`+`MathTradeItem`, resultados con toggle Por cadena/Por usuario. Incluye known issue del race de SectionGate
- [Code style and workflow preferences](feedback_style.md) — commit-msg/UI language, BGG warning, CSS Modules
- [Tests are required for every new component and method](feedback_tests_required.md) — non-negotiable since 2026-05-18: every new component, route, hook, util, or method ships with its tests in the same change (client AND server)
- [Page layout standard](padding_system.md) — padding variables + full-width rule (NO max-width caps on page wrappers, even long-form/legal) + editorial hero header pattern every new screen follows
- [Skeleton shimmer pattern](skeleton_pattern.md) — standard for all loading states; which screens are done, which are pending (BggProfile skipped)
- [Mobile nav drawer (replaces BottomNav)](feedback_mobile_nav_drawer.md) — Sidebar/GuestSidebar is a slide-in right drawer on mobile, toggled by a hamburger in the Navbar that morphs to X via CSS. Navbar z-index 250 sits above drawer 200 + backdrop 199 and acts as the visual header. Two close paths required (useEffect[pathname] + onClick={onClose}) — same-route Link clicks don't fire the effect alone.
- [PWA service worker config](feedback_pwa_sw_config.md) — workbox MUST have `skipWaiting + clientsClaim + cleanupOutdatedCaches` + `navigateFallbackDenylist: [/^\/api\//]`. Without these, deploys leave users on a stale cache → white screen after they clear browser cache.
- [Mobile controls + search pattern](feedback_mobile_controls_pattern.md) — list pages (Mesas, Eventos) stack `.controls` vertically on mobile, search takes full width on top, chips/view/CTA below. Inline `newBtn` hides → FAB takes over. Search input ALWAYS wraps with `.searchWrap` + absolute `.searchIcon` + `padding-left: 34px`.
- [FAB positioning post-BottomNav](feedback_fab_positioning.md) — every mobile FAB sits at `bottom: calc(16px + env(safe-area-inset-bottom, 0px))`. The 56-76px offset that existed to clear the BottomNav pill is dead — don't reintroduce it.
- [Deleted user UI convention](feedback_deleted_user.md) — null populated user refs render as "Usuario eliminado" via UserRef/getUserDisplay; never access user.username directly
- [VSCode ESLint setup](reference_vscode_eslint.md) — per-machine `.vscode/settings.json` pointing ESLint extension at `./client` (flat config lives there, not at repo root)
- [Plans folder location](feedback_plans_location.md) — Plans go in the project's `plans/` folder, not `~/.claude/plans/`
- [Admin "view as user" mode](feedback_admin_view_as_user.md) — admin-only features must respect the `viewAsUser` toggle (filter/hide privileged data client-side)
- [Panel admin toggles](feedback_panel_admin_toggles.md) — every new section or cross-cutting feature must be plumbed through `/panel-admin` (SECTION_KEYS, requireSection, SectionGate, SECTION_META)
- [Theme support](feedback_theme_support.md) — every new feature/screen must work in both dark and light themes; use CSS variables, never hardcoded colors
- [Dev scripts](reference_dev_scripts.md) — root has `npm run dev` (concurrently) + `dev:server` / `dev:client`; CLAUDE.md only documents the split form
- [No worktrees](feedback_no_worktrees.md) — user doesn't want to work in `.claude/worktrees/`; work directly in `~/Projects/turnocero/` and use feature branches there
- [Cyberpunk glitch effect](feedback_cyberpunk_glitch.md) — aesthetic para entradas impactantes (magenta+cyan RGB split, scanlines, beam, `steps(1)`); ver ChatWindow.module.css + gotchas en el archivo
- [BGG cache pattern](feedback_bgg_cache_pattern.md) — toda integración con BGG usa memoria → Mongo → BGG con `?refresh=1` y botón cliente con cooldown 60s
- [BGG sync engine (Phase 4)](feedback_bgg_sync_engine.md) — probe page-1 + reconcile dirigido + reconcile completo cada 30d + hash drift; reemplaza el wipe-and-refetch destructivo
- [Prefer BggPlay aggregations over collection](feedback_bgg_prefer_plays_aggregation.md) — toda vista derivada (top games, stats, listas) debe salir de aggregations sobre BggPlay; la colección omite plays unowned y rompe con perfiles privados
- [BGG username case mismatch](feedback_bgg_username_case.md) — `User.bggUsername` case-preserved vs `BggPlay.bggUsername` lowercase; cross-model queries necesitan `collation strength: 2`
- [withUserLock semantics](feedback_user_lock_semantics.md) — dedupe por key, no por work function; triggers de trabajos distintos deben ser mutuamente exclusivos en el caller
- [BGG write quirks](feedback_bgg_write_quirks.md) — `/xmlapi2/plays?id=` es game-id (no play-id); `geekplay.php` DELETE necesita `finalize=1`+`B1=Yes` o devuelve HTML de confirmación silenciosamente
- [Socket handler race](feedback_socket_handler_race.md) — todos los `socket.on(...)` en `io.on('connection')` deben registrarse ANTES de cualquier `await`; si vienen después de la auth lookup, los emits del cliente durante `connect` se descartan silenciosamente y el socket queda fuera del room
- [PUT partial update](feedback_put_partial_update.md) — endpoints PUT sólo deben modificar campos PRESENTES en `req.body`; el patrón viejo `field = body.field || undefined` clobereaba campos no enviados, rompiendo cualquier call parcial
- [Derived counts en UI](feedback_derived_counts.md) — counters derivables de un array (pendientes/confirmadas/...) se calculan con `useMemo`, no se mantienen en state; evita doble-conteo entre optimistic update y socket listener
- [Optimistic update vs socket race](feedback_optimistic_vs_socket.md) — si el server emite por socket al actor, el optimistic numérico se duplica; dejar el socket como fuente única, optimistic solo para asignaciones
- [Eventos section design](feedback_eventos_section_design.md) — rediseño mayo 2026: timeline+poster, ticket stub, 3-column triage, cancel reversible, socket rooms `evento:<id>` + `eventos:list`, cron 24h en `server.js` (NO `app.js`), solo in-app notifs
- [Google Maps setup](feedback_google_maps_setup.md) — reemplazó Leaflet+Nominatim (2026-05); `AddressMap`+`PlaceAutocomplete` shared, server-side geocode con cache Mongo, 2 Map IDs (dark/light), `getLocationDisplay(loc, mode)` para displayName
- [Debounce de inputs → API](feedback_debounce_inputs.md) — todo input cuyo value alimente un fetch debe usar el hook `useDebouncedValue` (300ms default, 500ms para fetches caros); no re-implementar `setTimeout` + `useRef` manual
- [Componentes shared de formularios](feedback_shared_form_components.md) — `<InfoTooltip>`, `<DateTimePicker>` (popover custom con auto-posicionamiento), status chips (radiogroup), convenciones de altura/radius/placeholder uniforme, regla "validación solo en handleSubmit, nunca HTML required" para evitar que el browser bloquee el submit nativo
- [Errores como toasts](feedback_errors_as_toasts.md) — errores PUT/POST/DELETE van por `addToast({ type: 'error' })`, no por state local `actionError` inline; contrato de re-throw solo si el caller necesita signal
- [Modal shared con focus mgmt](feedback_shared_modal.md) — todo overlay full-screen usa `components/shared/Modal.jsx` (role=dialog + aria-modal + Escape + focus trap + restauración)
- [Validar ObjectId en params](feedback_validate_objectid_param.md) — `router.param('id', validateObjectId)` al tope de cualquier router con `:id`; devuelve 400 antes que Mongoose tire CastError 500
- [Cron idempotency con flag en doc](feedback_cron_idempotency_flag.md) — crons "una vez por X" usan `xxxSentAt: Date` + ventana amplia, NO ventanas estrechas que se pierdan si el cron se atrasa
- [Test credentials](reference_test_credentials.md) — login de prueba para verificar páginas autenticadas en el preview/dev server
- [react-router-dom useSearchParams no es reactivo](feedback_react_router_search_params.md) — `setSearchParams` actualiza la URL pero NO dispara re-render; usar `useState` + `useLocation` + `history.replaceState` para sincronizar query params reactivos
- [Ludoteca + Mesas del Evento](project_ludoteca_de_eventos.md) — feature mergeada a master (commits 9290998, 408291e, aa1b29c, d201fee, 875e89d): tabs en EventoDetail, `Evento.ludoteca` embedded + `Table.eventoId`, `canActInEvento` helper, cascade de cancel, 2 tipos nuevos de notif
- [Modal shared usa portal — NO inline](feedback_modal_portal_required.md) — `<Modal>` renderiza con `createPortal(..., document.body)`; ancestros con `will-change: transform` + `overflow: clip` (ej. `.main` en EventoDetail) clippeaban el backdrop fixed
- [BGG: usar `image` (high-res) en renders ≥150px](feedback_bgg_image_over_thumbnail.md) — server siempre persistió `thumbnail` + `image` en BggGame/BggCollection/Evento.ludoteca; faltaba consumir `image` del lado cliente para evitar pixelado
- [asyncHandler + errorHandler pattern](feedback_async_handler_pattern.md) — desde commit a85eeb5 (P1.6) — `asyncHandler(fn)` + `throw httpError(status, msg)` + middleware central. **Migración cerrada: los 19 routers usan el patrón**. Documentado: casos especiales con try/catch interno (OG endpoints, anti-leak responses, cooldowns con headers, ValidationError); gotcha de 5xx explícitos con flag `isExplicit`
- [API endpoints centralization](feedback_api_endpoints_pattern.md) — desde 2026-05-22 (P4.4) — `client/src/api/endpoints.js` es fuente única de paths HTTP; usar `API.x.Y` / `API.x.Y(id)`. Migración completa al 2026-05-22 (56 archivos)
- [Tech debt audit progress](project_tech_debt_progress.md) — `plans/tech-debt-audit.md` cerrado 100% al 2026-05-23 (todos los items P0-P4); 819 server + 1560 client tests al cierre
- [Service layer pattern](feedback_service_layer.md) — desde 2026-05-22 — business logic vive en `server/services/` (bgg/aggregations|search|cache|cooldown|parse|resolve|syncEngine|mutations, torneoService, eventoService); routers son HTTP plumbing delgado; tests unit van en `tests/unit/services/`
- [Shared helpers catalog](feedback_shared_helpers_catalog.md) — catálogo de hooks/utils compartidos: client (`useApi`, `useShowcaseTables`, `getErrorMessage`, `passwordValidation`, `storageKeys`) + server (`idCompare`, `paginate`, `socketHelpers`, `regex`, `clamp`, `logger`, `cors config`); antes de inventar un helper, mirar acá
- [NotificationContext architecture](feedback_notifications_architecture.md) — desde 2026-05-22 (P1.1) — splitteado en 3 capas: contexto orquestador, `notificationReducers.js` (funciones puras testeables), `notificationListeners/` (9 hooks por dominio). Cambios de behavior van en reducers; eventos nuevos en listeners
- [Rate limiting per-user](feedback_user_rate_limit.md) — desde 2026-05-22 (P4.5) — `server/middleware/userRateLimit.js` factory para endpoints authed caros; key por `req.user._id` (no IP, porque NAT). Aplicado a BGG sync/mutations + eventos inscribirse/ludoteca. Skip en NODE_ENV=test
- [Cron lease distribuido](feedback_cron_lease.md) — desde 2026-05-22 (P4.6) — `withLease(name, fn)` desde `utils/cronLease.js` envuelve todo cron que muta data. Mongo doc con TTL evita que N instancias del server disparen el mismo tick. Reemplaza el "solo idempotency flag" anterior que no cubría tick simultáneo
- [AbortController en fetch effects](feedback_abort_controller_pattern.md) — desde 2026-05-23 — todo `useEffect` con `axios.get + setState` debe usar `AbortController` + `signal: ac.signal` + `axios.isCancel(err)`; evita stale-data races al navegar rápido entre detalles
- [Back button con history.state.idx](feedback_router_back_button_idx.md) — usar `window.history.state?.idx > 0` (no `location.key`) para distinguir navegación in-app vs entrada directa, y caer a un fallback razonable cuando no hay historial
- [Patrón de CTA primario](feedback_primary_cta_pattern.md) — desde 2026-05-26 — todo botón primario amber (`btn|cta|fab|submit`) usa el clon del "Nuevo evento" del handoff: base con `box-shadow: 0 4px 16px var(--amber-glow)` + `transition: all var(--transition)`, hover sube a `0 4px 22px` y bg=`amber-light` con color explícito; sin variar entre `0 4px 14px`, `0 6px 18px`, etc.
- [Mongo: elegir "el último" desempata por _id](feedback_mongo_latest_tiebreak.md) — toda aggregation que toma el más reciente (`$sort`+`$first` / top-N) debe ordenar por `{ createdAt: -1, _id: -1 }`; con timestamps empatados (inserts en ráfaga) el orden es indefinido → flaky. ObjectId monótono ⇒ `_id` rompe el empate hacia el insertado último. Fix real en `routes/dm.js`
- [Server markRead debe resetear count: 0](feedback_notif_markread_count_reset.md) — desde 2026-05-26 — `PATCH /api/notifications/read` tiene que `$set: { read: true, count: 0 }`, no solo `read: true`; sino el siguiente `$inc` de saveNotification se infla y el badge muestra valores stale (bug del "4 al recibir 1 notif"). Incluye regression test + nota del fix paralelo del test-setup express-rate-limit destructuring.
- [Privacy "Amigos" en mesas + gating + notifs join/leave](project_mesa_amigos_privacy.md) — 2026-05-28: `Table.privacy` enum expandido a `public|friends|private`; helper `tablePrivacy.js` central para visibilidad + 4 asserts de gating social (`assertCanComment/Rate/Follow/Linkable`); friends → 404 a no-amigos; comments/ratings/share/follow deshabilitados en non-public; followers legacy quedan inertes (no `spot_opened`/`image`/`table_cancelled` si non-public); nuevos notifs `player_joined` (public+friends) y `player_left` (todas las privacies). Incluye checklist completo para agregar nuevos notifs de mesa.
- [No icon libs — usar SVGs inline](feedback_inline_svg_icons.md) — `lucide-react`/`react-icons`/`@heroicons` NO están instalados; iconos como SVG inline (patrón Sidebar ICONS) o emoji
- [Meeple = viñeta de marca (reemplaza ◆)](feedback_meeple_brand_bullet.md) — desde 2026-06-01 el rombo `◆` se reemplazó por `<Meeple>` (SVG inline, currentColor, em) en ~52 archivos; usar `<Meeple/>` para nuevos eyebrows, no `◆`
- [Compartir: deeplink una sola vez](feedback_share_deeplink_once.md) — usar `utils/share.js#buildCompartidaShare` → `{ url, caption, whatsappText }`; Telegram pasa `url=` + `text=caption` (caption SIN url), nunca la url en ambos o se duplica
- [Marca "TurnoCero" (C mayúscula)](feedback_brand_name_turnocero.md) — todo texto user-facing usa "TurnoCero"; NO tocar el token técnico minúscula `turnocero` (folders Cloudinary, localStorage keys, env, repo)
- [Lazy loading / infinite scroll para listas grandes](feedback_lazy_loading_pattern.md) — toda lista que pueda crecer (comentarios, mensajes, feeds) pagina en server (`parsePagination` → `{ items, total, page, pages }`) y carga de a tandas en cliente con el hook `useInfiniteScroll` (sentinel IntersectionObserver, `root` opcional = caja scrolleable con `max-height`+`overflow:auto`); contadores del `total` del server, no `items.length`; botón fallback testeable
- [Rediseño Notificaciones](project_notificaciones_redesign.md) — 2026-05-30 (rama `redesign-notificaciones`): bandeja transversal con `utils/notifDomains.js` (type→dominio+color+icono+copy), `NotifRow`/`ResolvedRow`/`SidePanel`/`NotifIcons`, acciones inline amistad+mesa, `Notification.actors[]` (avatares apilados + userId del solicitante) vía pipeline en `saveNotification`, `DELETE /api/notifications/:id` + `dismiss()`. Excluido: Preferencias y `evento_pending`
- [Login OAuth Google/Facebook](project_oauth_login.md) — 2026-06-01 (rama `feature/oauth-login`): token-based sin Passport; rutas `/api/auth/oauth/{google,facebook}`, `oauthService` find-or-create con vinculación por email + username auto-generado, `User.googleId/facebookId` partial-index unique + password condicional, `OAuthButtons` shared, hook `useFacebookSdk`. Gotcha tests: mock `google-auth-library` vía require.cache
- [Rediseño login/register + color de avatar](project_auth_redesign.md) — 2026-06-01 (handoff `design_handoff_login`): `Auth.jsx` unificado (prop `mode`, Login/Register son wrappers), toggle = `<Link>` navegables; CSS con namespace propio anexado para no pisar las clases legacy que comparten Verify/Forgot/Reset/OAuth; `ShowcaseCard` extraído; `AuthShowcaseScene` SVG forzado-oscuro + `useShowcaseTables({refreshMs})`; color de avatar (`User.avatar.color` token de paleta, `utils/avatarColors.js`) elegible en registro + /perfil vía `AvatarColorPicker`; `<Avatar>` lo usa sobre el hash; OAuthButtons gatea Google por `VITE_GOOGLE_CLIENT_ID` (no white-screen sin env)
- [Pantallas de error 404/500](project_error_screens.md) — 2026-06-01 (handoff `design_handoff_errores`): `pages/error/ErrorScreen.jsx` base portaleada (variant 404/500, theme-aware, título imperativo no-Helmet), `NotFound` en catch-all, `ServerError` + `ErrorBoundary` global. El 500 dobla como **takeover de backend-caído**: health-check de boot vía `SiteConfigContext.backendDown` (`GET /api/site-config` falla por network/5xx ⇒ caído; 4xx = vivo), `AppShell` early-return a `<ServerError onRetry={retryConnection}/>`
- [react-helmet-async roto en 3.0.0](feedback_helmet_version_react18.md) — 2026-06-01: el 3.0.0 NO aplica ningún tag al head bajo React 18 (títulos/OG de todas las páginas quedaban en el default de index.html). Fijado en `^2.0.5`; no subir a 3.x hasta migrar a React 19. No es StrictMode
- [Chrome del shell sigue displayLocation](feedback_chrome_follows_displaylocation.md) — 2026-06-01: sidebars/navbar/frame deben gatearse con `transition.displayLocation.pathname` (hook `usePageTransition`), no con `useLocation()` vivo, o se desincronizan durante el slide (sidebar desaparecía antes de cargar el login). Chrome autenticado lleva `!isAuthPage`; fast-path de reduced-motion para no colgar el swap
